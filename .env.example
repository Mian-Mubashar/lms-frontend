# Local: leave unset — Vite proxy sends /api → http://localhost:5000 (see vite.config.js).
# Vercel (frontend project): REQUIRED — your deployed backend + /api (no trailing slash issues if one slash before api).
VITE_API_URL=https://your-lms-backend.vercel.app/api
